import 'package:flutter_test/flutter_test.dart';

// This file used to be a broken mockito-generated stub referencing a different
// package name (vraiauth). Replace it with a minimal placeholder test so the
// analyzer and CI stay green without codegen.

void main() {
  test('placeholder for removed mock file', () {
    expect(1 + 1, 2);
  });
}
